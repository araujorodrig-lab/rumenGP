# Parse ANKOM timestamps

Converts ANKOM RF timestamps into elapsed incubation time (hours).

## Usage

``` r
parse_ankom_time(time_raw)
```

## Arguments

- time_raw:

  Character vector containing ANKOM timestamps.

## Value

Numeric vector of elapsed incubation time in hours.
